server:
  port: 8082

spring:
  datasource:
    username: root
    password: root1234
    url: jdbc:mysql://localhost:3306/Facebook

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true


  application:
    name: user-service

